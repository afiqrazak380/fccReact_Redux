class DisplayMessages extends React.Component {
  constructor(props) {
    super(props);

    // Initialize the compenent state
    this.state = {
      input: '',
      messages: [],
    };
  }

  // Render method
  render() {
    return <div />;
  }
}
